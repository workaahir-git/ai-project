{
  "name": "gymcoach-backend",
  "version": "1.0.0",
  "description": "GymCoach Studio backend — receives form prompt, calls Grok, returns plan",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
